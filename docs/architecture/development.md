# Development Guidelines

All development work must be driven by the approved issue backlog.

No implementation should introduce implicit behavior, hidden coupling, or
cross-domain leakage.

Tooling, formatting, and linting are enforced consistently across the monorepo.
