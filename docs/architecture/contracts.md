# API Contracts

Zod schemas are the authoritative source of truth for API contracts.

OpenAPI documentation is generated from application code and must not be
manually edited.

Any change to API behavior requires an explicit contract update before
implementation.

