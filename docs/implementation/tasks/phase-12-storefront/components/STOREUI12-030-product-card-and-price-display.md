# Task: STOREUI12-030 — Product Card + Price Display

## Purpose

Create reusable product listing UI components for home/search/category pages.

## Implementation Notes

In `apps/storefront/app/components/*` implement:
- `ProductCard`
- `Price`
- `ProductBadge` (e.g., new, sale where supported)

Constraints:
- Never imply inventory guarantees from search projections.

## Acceptance Criteria

- Product cards render consistently across listing surfaces.
- Price display supports currency formatting.
