# Task: ADMINUI10-010 — Admin Design System Bindings

## Purpose

Define platform-admin UI component conventions using `@spareparts/ui` + Tailwind.

## Implementation Notes

- Establish shared page shell components:
  - `AdminPageHeader`
  - `AdminSection`
  - `AdminCard`
- Ensure consistent spacing/typography.

## Acceptance Criteria

- New pages share a consistent visual hierarchy.
- Components are reusable across routes.
