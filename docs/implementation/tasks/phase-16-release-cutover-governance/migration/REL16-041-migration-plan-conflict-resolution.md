# Task: REL16-041 — Migration Plan + Conflict Resolution

## Purpose

Define a migration plan that preserves meaning and resolves conflicts explicitly (Domain AG).

## Implementation Notes

- For each migrated dataset:
  - source-of-truth
  - transformation rules (meaning-preserving only)
  - conflict policy (explicit, not silent)
  - incomplete history handling (visibly incomplete)

## Acceptance Criteria

- Migration plan is explicit and reviewable.
- Conflicts are handled explicitly.
